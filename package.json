{
  "name": "reachinbox-scheduler-app",
  "version": "1.0.0",
  "private": true,
  "description": "ReachInbox Email Scheduler - Production Ready Full Stack Application",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build:frontend": "npm run build --prefix frontend",
    "build:backend": "npm run build --prefix backend",
    "build": "npm run build:frontend && npm run build:backend",
    "start": "npm start --prefix backend",
    "render-build": "cd frontend && npm install && npm run build && cd ../backend && npm install && npm run build && npx prisma db push"
  }
}
